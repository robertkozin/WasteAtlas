---
import { getCollection } from "astro:content"
import BaseLayout from "../layouts/BaseLayout.astro"
import DirectoryPage from "@components/dir/DirectoryPage.astro"
import { allWastes } from "./_data"

---

<BaseLayout title="Waste Atlas Directory">
  <DirectoryPage wastes={allWastes} />
  <!-- aquí se pasa la info de la database a la página de directorio para que tambien tenga acceso a los residuos -->
</BaseLayout>
